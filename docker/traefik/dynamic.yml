# Edgecontrol — Traefik file provider. The API overwrites this file with managed service routes.
# Panel + API ingress use Docker labels on the `web` and `api` services (PANEL_HOST / API_HOST in .env).
http:
  routers: {}
  services: {}
  middlewares: {}
