# Edgecontrol — Traefik file provider. The API overwrites this file with managed service routes
# (HTTPS + Let’s Encrypt on :443). The panel and core API are not defined here — they use
# published host ports (see docker-compose.yml: PANEL_PUBLISH_PORT / API_PUBLISH_PORT).
http:
  routers: {}
  services: {}
