# Edgecontrol — Traefik file provider. The API overwrites this file with managed service routes
# (HTTPS + Let’s Encrypt on :443). Traefik v3.6 rejects empty `routers`/`services`/`middlewares` maps;
# use `http: {}` until routes exist, then the API writes non-empty `http.routers` and `http.services`.
http: {}
